

// Je commence par créer une div pour les filtres
// Une seule fonction pour créer les 3 listes de tags
// Je dois faire une div de la taille des filtres
// Je mets en paramètres le titre et la liste des éléments avec tri des doublons
// Je vais créer une div de titre et de bouton fléche
// ajouter un listener sur la flèche pour ouvrir le déroulant + effet rotation + passer en affichage des éléments => input = list tags

// Création et ajout dans le DOM de l'input
// add.EventListener sur l'input de filter => on écoute quand on tape et on regarde le change quand entrer
// On veut filter la liste liste de manière dynamique au fur et à mesure de la saisie

// et aussi création est ajout dans le DOM de la list des tags ( possible d'utiliser des p / h4 etc)
// onClick sur un tag => une création d'une div en jaune portant le titre nom du tag => s'ajoute entre l'input et la liste
// + sur la même fonction => vider l'input + on remet la liste mais on retire l'élément sélectionner
// + ajout d'une étiquette portant le texte sélectionner

// quand je clique sur la croix de l'étiquette => je le remets dans la liste